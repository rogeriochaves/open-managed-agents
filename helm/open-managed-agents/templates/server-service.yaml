apiVersion: v1
kind: Service
metadata:
  name: {{ include "oma.fullname" . }}-server
  labels:
    {{- include "oma.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.server.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oma.server.selectorLabels" . | nindent 4 }}
