apiVersion: v1
kind: Service
metadata:
  name: {{ include "oma.fullname" . }}-web
  labels:
    {{- include "oma.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oma.web.selectorLabels" . | nindent 4 }}
